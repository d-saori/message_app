<%# @messageは既にデータベースに入ってるデータを渡している。「model: @message」はupdateに飛ばすものだと理解してくれるので他にURIなど書かなくてOK %>
<%= form_with model: @message, local: true do |form| %>
  <%= form.text_field :content, required: true %>
  <%= form.submit "送信" %>
<% end %>
